# My First Note

This is a test note to verify that the dynamic notes feature is working!
When you add new `.txt` or `.md` files to the `notes/` folder, they will automatically be parsed by `generate_notes.py`.

The first line starting with a `#` is used as the title.
The rest is the body of the note.

It even supports multiple paragraphs.
